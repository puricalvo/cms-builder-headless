---
export const prerender = false;

import ProductsGrid from "@/components/products/ProductsGrid.astro";
import OrderLayout from "@/layouts/OrderLayout.astro";

/* const token = Astro.cookies.get("FRESHCOFFEE_TOKEN")?.value;

if (!token) {
    return Astro.redirect("/auth/login");
} */
---

<OrderLayout>
    <ProductsGrid />
</OrderLayout>